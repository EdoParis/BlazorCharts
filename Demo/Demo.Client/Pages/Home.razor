@page "/"

<PageTitle>Home</PageTitle>

<table style="width: 100%;">
    <tr>
        <td><LineChart Model="@linegram"></LineChart></td>
        <td><HistChart Model="@histogram" Color="@KnownColor.SeaGreen" OnClick="BinClickHandler"></HistChart></td>
    </tr>
    <tr>
        <td><BarChart Model="@bargram" Color="@KnownColor.RoyalBlue" Direction="@Positioning.Vertical" OnClick="BarClickHandler"></BarChart></td>
        <td><BarChart Model="@bargram" Color="@KnownColor.PaleVioletRed" Direction="@Positioning.Horizontal" OnClick="BarClickHandler"></BarChart></td>
    </tr>
    <tr>
        <td><RadarChart Model="@radargram" Color="KnownColor.MediumVioletRed"></RadarChart></td>
        <td><PolarChart Model="@polargram" OnClick="SliceClickHandler"></PolarChart></td>
    </tr>
    <tr>
        <td><PieChart Model="@piegram" OnClick="SliceClickHandler"></PieChart></td>
        <td><DonutChart Model="@piegram" OnClick="SliceClickHandler"></DonutChart></td>
    </tr>
</table>

<ol>
    @foreach (string message in events)
    {
        <li>@message</li>
    }
</ol>

